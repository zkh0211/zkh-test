#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_treer@1.0.4@treer/src/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../_treer@1.0.4@treer/src/index.js" "$@"
  ret=$?
fi
exit $ret
