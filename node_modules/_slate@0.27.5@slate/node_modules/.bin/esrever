#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_esrever@0.2.0@esrever/bin/esrever" "$@"
  ret=$?
else 
  node  "$basedir/../../../_esrever@0.2.0@esrever/bin/esrever" "$@"
  ret=$?
fi
exit $ret
