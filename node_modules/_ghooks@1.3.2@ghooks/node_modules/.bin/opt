#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_opt-cli@1.5.1@opt-cli/bin/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_opt-cli@1.5.1@opt-cli/bin/index.js" "$@"
  ret=$?
fi
exit $ret
