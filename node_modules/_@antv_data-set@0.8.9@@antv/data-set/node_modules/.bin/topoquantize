#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_topojson-client@3.0.0@topojson-client/bin/topoquantize" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_topojson-client@3.0.0@topojson-client/bin/topoquantize" "$@"
  ret=$?
fi
exit $ret
